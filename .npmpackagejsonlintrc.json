{
  "rules": {
    "prefer-absolute-version-dependencies": [
      "error",
      {
        "exceptions": [
          "lib"
        ]
      }
    ],
    "prefer-absolute-version-devDependencies": "error",
    "prefer-absolute-version-peerDependencies": "error",
    "prefer-absolute-version-optionalDependencies": "error"
  }
}
